---
category: Hooks
alias: useWidgetStorage
source: src/hooks/use-widget-storage.ts
---

# useWidgetStorage

持久化 widget 本地状态。

Learn more at `src/hooks/use-widget-storage.ts`.

## Usage

```ts
import { useWidgetStorage } from '@widget-js/react'

export function Demo() {
  const state = useWidgetStorage(/* options */)
  return state
}
```

## Type Declarations

```ts
export function useWidgetStorage(...args: unknown[])
```

## API Snapshot

- 导入：`import { useWidgetStorage } from '@widget-js/react'`

