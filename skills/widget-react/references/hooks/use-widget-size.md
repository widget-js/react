---
category: Hooks
alias: useWidgetSize
source: src/hooks/use-widget-size.ts
---

# useWidgetSize

读取 widget 尺寸与样式尺寸。

Learn more at `src/hooks/use-widget-size.ts`.

## Usage

```ts
import { useWidgetSize } from '@widget-js/react'

export function Demo() {
  const state = useWidgetSize()
  return state
}
```

## Type Declarations

```ts
export function useWidgetSize()
```

## API Snapshot

- 导入：`import { useWidgetSize } from '@widget-js/react'`

