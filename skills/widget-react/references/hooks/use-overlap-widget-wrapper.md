---
category: Hooks
alias: useOverlapWidgetWrapper
source: src/hooks/use-widget-wrapper.ts
---

# useOverlapWidgetWrapper

overlap 包装层纵向过渡。

Learn more at `src/hooks/use-widget-wrapper.ts`.

## Usage

```ts
import { useOverlapWidgetWrapper } from '@widget-js/react'

export function Demo() {
  const state = useOverlapWidgetWrapper()
  return state
}
```

## Type Declarations

```ts
export function useOverlapWidgetWrapper()
```

## API Snapshot

- 导入：`import { useOverlapWidgetWrapper } from '@widget-js/react'`

