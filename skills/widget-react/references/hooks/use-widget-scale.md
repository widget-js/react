---
category: Hooks
alias: useWidgetScale
source: src/hooks/use-widget.ts
---

# useWidgetScale

只做缩放比例计算。

Learn more at `src/hooks/use-widget.ts`.

## Usage

```ts
import { useWidgetScale } from '@widget-js/react'

export function Demo() {
  const state = useWidgetScale(/* options */)
  return state
}
```

## Type Declarations

```ts
export function useWidgetScale(width: number,
  height: number,
  widgetWidth: number,
  widgetHeight: number,)
```

## API Snapshot

- 导入：`import { useWidgetScale } from '@widget-js/react'`

