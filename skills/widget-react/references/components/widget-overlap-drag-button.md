---
category: Components
alias: WidgetOverlapDragButton
source: src/components/widget-overlap-drag-button.tsx
---

# WidgetOverlapDragButton

overlap 模式下的拖拽入口按钮。

Learn more at `src/components/widget-overlap-drag-button.tsx`.

::: tip
- `WidgetWrapper` 已经内置这个组件，多数场景不需要手动单独接。
:::

## Usage

```tsx
import { WidgetOverlapDragButton } from '@widget-js/react'

export function Demo() {
  return (
    <WidgetOverlapDragButton />
  )
}
```

## Type Declarations

```tsx
export function WidgetOverlapDragButton()
```

## API Snapshot

- 导入：`import { WidgetOverlapDragButton } from '@widget-js/react'`

